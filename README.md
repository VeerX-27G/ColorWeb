This web app extracts all the types of colors that are in the image uploaded by you.

Project has python as backend, and HTML and CSS as frontend.

Flask is for handling .html pages and Jinja2 templates.
PIL (pillow) is used to access that image you will be uploading and converting it to RGB colors.
NumPy is to make a 3D array of image of that uploaded image.
